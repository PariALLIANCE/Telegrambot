name: Fetch UEFA & CWC Matches

on:
  workflow_dispatch:   # Exécution manuelle
  schedule:
    - cron: '0 3 * * *'  # Tous les jours à 03h UTC

permissions:
  contents: write

jobs:
  fetch:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout du dépôt
        uses: actions/checkout@v4

      - name: 🐍 Installer Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: ⚙️ Installer dépendances
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4

      - name: 🔎 Exécuter ligues.py
        run: |
          python ligues.py

      - name: 💾 Commit et Push des mises à jour
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add .
          git commit -m "Mise à jour automatique des matchs UEFA & CWC" || echo "Aucun changement à valider"
          git push
